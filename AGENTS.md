# cn-skills-cli

中国优化的 `npx skills` 平替——从 GitHub / Gitee / GitLab 安装 AI agent skills，带 Gitee 镜像加速与中文索引，让国内用户免翻墙安装。

## Agent skills

### Issue tracker

Issues 与 specs 存放在 `SHYXIN/cn-skills-cli` 的 GitHub Issues（使用 `gh` CLI）。详见 `docs/agents/issue-tracker.md`。

### Triage labels

七个规范角色（2 类目 + 5 状态），label 名与角色名一致。详见 `docs/agents/triage-labels.md`。

### Domain docs

Single-context：仓库根的 `CONTEXT.md` 加 `docs/adr/`。详见 `docs/agents/domain.md`。
